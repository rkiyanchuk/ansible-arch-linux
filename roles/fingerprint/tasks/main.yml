---
- name: install fprintd
  pacman:
    name:
      - fprintd

- name: install polkit policy for fprintd
  copy:
    src: polkit/50-net.reactivated.fprint-enroll.rules
    dest: /usr/share/polkit-1/rules.d/50-net.reactivated.fprint-enroll.rules
