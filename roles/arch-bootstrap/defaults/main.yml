arch_root: /mnt

# Define storage partitioning layout.
# Required fields for partition:
#   - name
#   - start
#   - end
#
# Optional fields:
#   - flags
#   - filesystem
#   - mountpoint
drive:
  device: /dev/sda
  table: gpt
  partitions:

    - name: efi
      start: 0%
      end: 512MiB
      flags:
        - boot
      filesystem: vfat
      mountpoint: /boot/efi
      encrypt: false

    - name: root
      start: 512MiB
      end: 100%
      filesystem: ext4
      mountpoint: /
      encrypt: true
